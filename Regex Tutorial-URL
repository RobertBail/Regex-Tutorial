# Regex Tutorial: Matching a URL
A Regex, or regular expression, refers to a series of characters that define a search pattern. This tutorial explains how a specific regular expression, matching a URL, functions by breaking down each component of the expression and describing its purpose.